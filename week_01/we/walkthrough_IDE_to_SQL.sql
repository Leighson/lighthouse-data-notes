/*
This is the output from the excercise of using VSCode
to connect to a database using the SQLite extension.

The query below--if no errors are present in the code--
should appear in a window beside the code view window.
*/

SELECT count(*) AS 'Number of Artists' FROM artists;

SELECT * FROM employees;